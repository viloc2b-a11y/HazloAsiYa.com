# Pages Functions: `node:crypto` (MD5 del subscriber hash) en subscribe-email
name = "hazloasiya"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "out"

# Cloudflare Pages: no uses bloques de cron en este archivo (solo Workers independientes).
# Cron para `functions/_scheduled.ts`: dashboard del proyecto Pages → Functions → crons,
# o un Worker aparte con su propio wrangler.
