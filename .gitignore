node_modules

# Secretos y entorno local
.env.local
.env*.local

# Output de análisis Python — no es el sitemap de producción
# El sitemap real está en out/sitemap.xml (generado por Next.js build)
seo_output/

# Reporte generado por migrate-content.mjs (regenerar con npm run migrate:audit)
scripts/migrate-report.json

# Next.js build outputs
.next
out

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor
.DS_Store
Thumbs.db

# Entregas / artefactos zip locales
*.zip

# Generado por npm run audit:data (regenerable)
docs/data-inventory-auto.json
